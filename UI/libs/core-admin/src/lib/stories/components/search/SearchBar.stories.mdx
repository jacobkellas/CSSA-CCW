<!-- SearchBar.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import SearchBar from '@core-admin/components/search/SearchBar';

<Meta
  title="Admin/Components/Search/SearchBar"
  component={SearchBar}
/>

# SearchBar

With `MDX`, we can define a story for `SearchBar` component right in the middle of our Markdown documentation.

<Props of={SearchBar} />

<Canvas>
  <Story name="Primary">
    {{
      components: { SearchBar },
      template: `<div>
                    <SearchBar />
                </div>`,
    }}
  </Story>
</Canvas>

## Search Bar

<Source
  language="js"
  dark
  format={true}
  code={dedent`
  <v-autocomplete
    v-model="model"
    :loading="isLoading"
    :search-input.sync="search"
    hide-no-data
    hide-selected
    item-text="Description"
    item-value="API"
    label="Search"
    placeholder="Start typing to Search"
    prepend-icon="mdi-magnify"
  ></v-autocomplete>
`}
/>
;
