<!-- AppointmentScheduler.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import AppointmentScheduler from '@core-admin/components/appointment/AppointmentScheduler.vue';

<Meta
  title="Admin/Components/Appointment/AppointmentScheduler"
  component={AppointmentScheduler}
/>

# AppointmentScheduler

With `MDX`, we can define a story for `AppointmentScheduler` component right in the middle of our Markdown documentation.

<Props of={AppointmentScheduler} />

<Canvas>
  <Story name="Primary">
    {{
      components: { AppointmentScheduler },
      template: `
      <div>
        <AppointmentScheduler />
      </div>`,
    }}
  </Story>
</Canvas>

## Fetching the Appointments

<Source
  language="js"
  dark
  format={true}
  code={dedent`
const { getAppointmentsApi } = useAppointmentsStore();
const { isLoading, isError, data } = useQuery(
  ['appointments'],
  getAppointmentsApi
);
`}
/>
;
