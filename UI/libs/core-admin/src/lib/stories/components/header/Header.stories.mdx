<!-- Header.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import Header from '@core-admin/components/header/Header';

<Meta
  title="Admin/Components/Header/Header"
  component={Header}
/>

# Header

With `MDX`, we can define a story for `Header` component right in the middle of our Markdown documentation.

<Props of={Header} />

<Canvas>
  <Story name="Primary">
    {{
      components: { Header },
      template: `<div>
                    <Header />
                </div>`,
    }}
  </Story>
</Canvas>

## Header App Bar

<Source
  language="js"
  dark
  format={true}
  code={dedent`
  <v-app-bar
    app
    color="primary"
    class="flex-grow-0"
    dark
  >
    <v-app-bar-title>
      {{ $t(' NavBar I am under construction') }}
    </v-app-bar-title>
  </v-app-bar>
`}
/>
;
