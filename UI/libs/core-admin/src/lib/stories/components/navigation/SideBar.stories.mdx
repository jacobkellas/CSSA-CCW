<!-- SideBar.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import SideBar from '@core-admin/components/navigation/SideBar';

<Meta
  title="Admin/Components/Navigation/SideBar"
  component={SideBar}
/>

# SideBar

With `MDX`, we can define a story for `SideBar` component right in the middle of our Markdown documentation.

<Props of={SideBar} />

<Canvas>
  <Story name="Primary">
    {{
      components: { SideBar },
      template: `<div>
                    <SideBar />
                </div>`,
    }}
  </Story>
</Canvas>

## Navigation Side Bar

<Source
  language="js"
  dark
  format={true}
  code={dedent`
  <v-navigation-drawer
    app
    v-model="drawer"
    :mini-variant.sync="$vuetify.breakpoint.mdAndDown"
    :clipped="$vuetify.breakpoint.mdAndUp"
    class="sidebar"
    permanent
  ><v-navigation-drawer>
`}
/>
;
