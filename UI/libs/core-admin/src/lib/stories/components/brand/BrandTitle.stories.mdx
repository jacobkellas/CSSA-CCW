<!-- BrandTitle.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import dedent from 'ts-dedent';
import BrandTitle from '@core-admin/components/brand/BrandTitle';

<Meta
  title="Components/BrandTitle"
  component={BrandTitle}
/>

# BrandTitle

With `MDX`, we can define a story for `BrandTitle` right in the middle of our Markdown documentation.

<Props of={BrandTitle} />

<Canvas>
  <Story name="Primary">
    {{
      components: { BrandTitle },
      template: `
          <div>
        <v-form>
          <BrandTitle />
        </v-form>
  </div>`,
    }}
  </Story>
</Canvas>

## Brand-able settings will be saved

<Source
  language="js"
  dark
  format={true}
  code={dedent`
function getFormValues(submitEvent) {
  // Push model JSON to API endpoint for Public App consumption"
  console.log(formJson(submitEvent.target));
}
`}
/>
;
