<!-- FormButtonContainer.stories.mdx -->

import {
  Meta,
  Canvas,
  Story,
  Props,
  Source,
} from '@storybook/addon-docs/blocks';
import { withKnobs, boolean } from '@storybook/addon-knobs';
import dedent from 'ts-dedent';

import FormButtonContainer from '@core-public/components/containers/FormButtonContainer';

<Meta
  title="Components/Containers/FormButtonContainer"
  component={FormButtonContainer}
  decorators={[withKnobs]}
/>

# Form Button Container

This is a `<FormButtonContainer>` component which has basic Continue, Save and Cancel buttons.

<Props of={FormButtonContainer} />

<Canvas>
  <Story
    name="Primary"
    height="400px"
  >
    {{
      components: { FormButtonContainer },
      template: '<FormButtonContainer valid={true} />',
    }}
  </Story>
</Canvas>

## Any form information enetered will be collected and stored

<Source
  language="js"
  dark
  format={true}
  code={dedent`
    function handleSubmit() {
        emit('submit');
    }
`}
/>
;
