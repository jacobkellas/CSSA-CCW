<!-- FormContainer.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import { withKnobs } from '@storybook/addon-knobs';
import dedent from 'ts-dedent';

import FormContainer from '@core-public/components/containers/FormContainer';

<Meta
  title="Components/Containers/FormContainer"
  component={FormContainer}
  decorators={[withKnobs]}
/>

# FormContainer

This is a `<FormContainer>` which allows user to wrap individual form elements inside container.

<Props of={FormContainer} />

<Canvas>
  <Story
    name="Primary"
    height="400px"
  >
    {{
      components: { FormContainer },
      template: `<div> <FormContainer /> </div>`,
    }}
  </Story>
</Canvas>

