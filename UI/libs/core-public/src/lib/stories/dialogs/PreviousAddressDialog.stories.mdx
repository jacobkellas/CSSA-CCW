<!-- PreviousAddressDialog.stories.mdx -->

import { Canvas, Meta, Props, Story, Source } from '@storybook/addon-docs';
import { withKnobs } from '@storybook/addon-knobs';
import dedent from 'ts-dedent';

import PreviousAddressDialog from '@core-public/components/dialogs/PreviousAddressDialog';

<Meta
  title="Components/Dialogs/PreviousAddressDialog"
  component={PreviousAddressDialog}
  decorators={[withKnobs]}
/>

# PreviousAddressDialog

This is a Previous Addresses Dialog component which allows user to enter previous address information in the form.

<Props of={PreviousAddressDialog} />

<Canvas>
  <Story
    name="Primary"
    height="400px"
  >
    {{
      components: { PreviousAddressDialog },
      template: `<div> <PreviousAddressDialog /></div>`,
    }}
  </Story>
</Canvas>

## Address info will be collected and stored into pinia

<Source
  language="js"
  dark
  format={true}
  code={dedent`
  function handleSubmit() {
    props.getPreviousAddressFromDialog(address);
    dialog.state = false;
  }
`}
/>
;
